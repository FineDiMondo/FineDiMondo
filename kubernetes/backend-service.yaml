apiVersion: v1
kind: Service
metadata:
  name: finedimondo-backend
  namespace: danielworkspace
  labels:
    app: finedimondo-backend
spec:
  type: ClusterIP
  selector:
    app: finedimondo-backend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
