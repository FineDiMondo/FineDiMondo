apiVersion: v1
kind: ConfigMap
metadata:
  name: finedimondo-config
  namespace: danielworkspace
data:
  # Backend Configuration
  API_PORT: "8080"
  API_HOST: "0.0.0.0"
  DEBUG: "true"

  # Database Configuration
  DB_HOST: "34.76.47.191"
  DB_PORT: "5432"
  DB_NAME: "kyuss_retro"

  # Vertex AI Configuration
  VERTEX_AI_LOCATION: "europe-west1"
  VERTEX_AI_MODEL: "gemini-2.5-flash"

  # Frontend Configuration
  FRONTEND_PORT: "80"
  API_URL: "http://finedimondo-backend:8080/api"

  # Storage
  BUCKET_NAME: "fine-di-mondo-posters"

  # Service URLs
  REGISTRY_URL: "registry.finedimondo.mywire.org"
